# 统一 Job 产物隔离与成果展示

## 目标

将视频下载、本地转写与 Pipeline 统一为同一种 Job 产物模型：每次提交使用 Job ID 独立目录，Job 内以 `items/001` 等隔离多个输入，并持久化 `job.json` 产物清单。任务中心直接按清单展示标题、来源 URL、视频与转写产物，旧任务仍可用目录扫描兜底。

## 阶段

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| 1. 审查当前任务与产物链路 | 已完成 | 已确认仅 Pipeline 有批次/子任务目录；下载与转写共享输出根目录。 |
| 2. 定义通用 Job 目录与清单 | 进行中 | 在执行脚本中建立 Job ID、items 和 `job.json` 产物记录。 |
| 3. 接入服务端任务创建与详情 | 待开始 | 为下载、转写、Pipeline 创建隔离输出目录；返回清单优先的详情。 |
| 4. 调整任务中心展示 | 待开始 | 使用标题与来源链接展示 item，兼容旧任务递归扫描。 |
| 5. 回归验证 | 待开始 | 检查、构建，并验证新旧任务详情数据。 |

## 关键决策

- Job ID 是三类任务唯一的身份；Pipeline 的原 batch ID 降级为可选展示名称，不作为身份。
- 新任务目录使用用户配置的输出根目录加 Job ID；一个 Job 内的多个输入使用 `items/001`、`items/002`。
- `job.json` 是新任务的产物事实来源，记录 item 标题、来源与实际产物；目录扫描仅兼容旧任务。
- 文件名不承担归属或展示语义；展示标题优先来自解析标题，来源 URL 作为可点击链接。

## 验证标准

- 新下载、本地转写、Pipeline 都创建独立 Job 输出目录。
- 一个 Job 内多个 URL/输入均按 item 展示；URL 来源可点击，文件来源可定位。
- `job.json` 记录实际输出的相对路径和类型；任务详情不靠文件名猜归属。
- 旧任务仍能通过递归扫描展示，且隐藏 `.DS_Store`、`task.json`、`batch.json`。
- TypeScript/Vite 编译通过。
