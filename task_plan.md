# 统一用户设置与任务页面精简

## 目标

将工作目录与可复用页面参数统一保存到 `.local-data/settings.json`；用户修改即持久化。移除工作目录历史和 Pipeline 的“仅下载、跳过转写”模式，并统一默认输出目录为 `videos/`、`transcripts/`、`pipeline/`。完成编译、启动检查并提交代码。

## 阶段

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| 1. 审查现有实现 | 已完成 | 已确认服务端配置、表单状态、任务参数和已有变更。 |
| 2. 统一设置持久化 | 已完成 | 已实现 `settings.json` 读写、默认值回退、旧配置迁移与页面即时保存。 |
| 3. 调整界面与任务语义 | 已完成 | 已移除目录历史和跳过转写，并更新默认目录。 |
| 4. 验证与提交 | 进行中 | 已通过检查与启动验证，正在审阅最终差异并提交。 |

## 关键决策

- `settings.json` 不存在时，以代码默认值为准；不在启动时创建空配置文件。
- 每次修改可复用设置立即写入；URL、媒体输入等一次性参数不持久化。
- 当前工作目录属于 `settings.workspace.workingDirectory`，不保留最近目录历史。
- “恢复默认设置”删除对应设置段，回退到代码默认值。

## 验证标准

- TypeScript/Vite 编译通过。
- 服务可正常启动。
- 用户未要求人工页面验证；不以此作为本次交付门槛。
